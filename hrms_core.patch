diff --git a/hrms/api/job_applicant_webform.py b/hrms/api/job_applicant_webform.py
new file mode 100644
index 0000000..a5940a0
--- /dev/null
+++ b/hrms/api/job_applicant_webform.py
@@ -0,0 +1,130 @@
+import frappe
+from frappe import _
+
+@frappe.whitelist()
+def get_job_applicant_by_email(email):
+    if not email:
+        frappe.throw(_("Email is required"))
+
+    applicant = frappe.get_all(
+        "Job Applicant",
+        filters={"email_id": email},
+        fields=[
+            "name",
+            "applicant_name", "email_id", "phone_number",
+            "country", "job_title", "designation",
+            "status", "source", "source_name",
+            "employee_referral", "applicant_rating",
+            "notes", "cover_letter",
+            "resume_attachment", "resume_link",
+            "currency", "lower_range", "upper_range"
+        ],
+        limit_page_length=1
+    )
+
+    return applicant[0] if applicant else {}
+
+
+@frappe.whitelist()
+def save_job_applicant(data):
+    import json
+    data = json.loads(data) if isinstance(data, str) else data
+
+    email = data.get("email_id")
+    if not email:
+        frappe.throw(_("Email ID missing"))
+
+    # Get the correct document using email
+    applicant = frappe.get_all(
+        "Job Applicant",
+        filters={"email_id": email},
+        fields=["name"],
+        limit_page_length=1
+    )
+
+    if applicant:
+        doc = frappe.get_doc("Job Applicant", applicant[0].name)
+        doc.update(data)
+        doc.save(ignore_permissions=True)
+        frappe.db.commit()
+        return {"status": "updated", "name": doc.name}
+
+    # Create new applicant
+    doc = frappe.get_doc({"doctype": "Job Applicant", **data})
+    doc.insert(ignore_permissions=True)
+    frappe.db.commit()
+    return {"status": "created", "name": doc.name}
+
+import frappe
+from frappe import _
+
+@frappe.whitelist()
+def get_job_applicant_by_email(email):
+    if not email:
+        frappe.throw(_("Email is required"))
+
+    applicant = frappe.get_all(
+        "Job Applicant",
+        filters={"email_id": email},
+        fields=[
+            "name",
+            "applicant_name", "email_id", "phone_number",
+            "country", "job_title", "designation",
+            "status", "source", "source_name",
+            "employee_referral", "applicant_rating",
+            "notes", "cover_letter",
+            "resume_attachment", "resume_link",
+            "currency", "lower_range", "upper_range"
+        ],
+        limit_page_length=1
+    )
+    return applicant[0] if applicant else {}
+
+
+# @frappe.whitelist()
+# def save_job_applicant(data):
+#     import json
+#     data = json.loads(data) if isinstance(data, str) else data
+
+#     email = data.get("email_id")
+#     if not email:
+#         frappe.throw(_("Email ID is required"))
+
+#     if email != frappe.session.user:
+#         frappe.throw(_("You can only update your own application"))
+
+#     allowed_fields = [
+#         "applicant_name",
+#         "phone_number",
+#         "lower_range",
+#         "upper_range",
+#         "resume_attachment"
+#     ]
+
+#     clean_data = {k: v for k, v in data.items() if k in allowed_fields}
+
+#     applicant = frappe.get_all(
+#         "Job Applicant",
+#         filters={"email_id": email},
+#         fields=["name"],
+#         limit_page_length=1
+#     )
+
+#     if applicant:
+#         doc = frappe.get_doc("Job Applicant", applicant[0]["name"])
+#         doc.update(clean_data)
+#         doc.save(ignore_permissions=True)
+#         frappe.db.commit()
+#         return {"status": "updated", "name": doc.name}
+
+#     clean_data.update({
+#         "doctype": "Job Applicant",
+#         "email_id": email,
+#         "status": "Open"
+#     })
+#     doc = frappe.get_doc(clean_data)
+#     doc.insert(ignore_permissions=True)
+#     frappe.db.commit()
+#     return {"status": "created", "name": doc.name}
+
+
diff --git a/hrms/hr/doctype/candidate_attachments/candidate_attachments.json b/hrms/hr/doctype/candidate_attachments/candidate_attachments.json
deleted file mode 100644
index 436d4d6..0000000
--- a/hrms/hr/doctype/candidate_attachments/candidate_attachments.json
+++ /dev/null
@@ -1,39 +0,0 @@
-{
- "actions": [],
- "allow_rename": 1,
- "creation": "2025-10-15 10:16:26.555856",
- "doctype": "DocType",
- "editable_grid": 1,
- "engine": "InnoDB",
- "field_order": [
-  "document_type",
-  "file"
- ],
- "fields": [
-  {
-   "fieldname": "document_type",
-   "fieldtype": "Select",
-   "label": "Document Type",
-   "options": "Experience Letter\nRelieving Letter\nSalary Slip"
-  },
-  {
-   "fieldname": "file",
-   "fieldtype": "Attach",
-   "label": "File"
-  }
- ],
- "grid_page_length": 50,
- "index_web_pages_for_search": 1,
- "istable": 1,
- "links": [],
- "modified": "2025-10-15 10:22:18.631985",
- "modified_by": "Administrator",
- "module": "HR",
- "name": "Candidate Attachments",
- "owner": "Administrator",
- "permissions": [],
- "row_format": "Dynamic",
- "sort_field": "modified",
- "sort_order": "DESC",
- "states": []
-}
\ No newline at end of file
diff --git a/hrms/hr/doctype/job_applicant/job_applicant.js b/hrms/hr/doctype/job_applicant/job_applicant.js
index 9cbdc77..8d79746 100644
--- a/hrms/hr/doctype/job_applicant/job_applicant.js
+++ b/hrms/hr/doctype/job_applicant/job_applicant.js
@@ -115,3 +115,5 @@ frappe.ui.form.on("Job Applicant", {
 		});
 	},
 });
+
+
diff --git a/hrms/hr/doctype/job_applicant/job_applicant.py b/hrms/hr/doctype/job_applicant/job_applicant.py
index bc7ca64..86c35ac 100644
--- a/hrms/hr/doctype/job_applicant/job_applicant.py
+++ b/hrms/hr/doctype/job_applicant/job_applicant.py
@@ -48,7 +48,22 @@ class JobApplicant(Document):
 				frappe.throw(
 					_("Cannot create a Job Applicant against a closed Job Opening"), title=_("Not Allowed")
 				)
-
+				
+	def has_webform_permission(self, permtype="read"):
+		"""Allow applicant to update record if email_id == logged-in user"""
+		user = frappe.session.user
+
+		# Block Guest access
+		if user == "Guest":
+			return False
+
+		# Allow if applicant email matches logged-in user email
+		if self.email_id and self.email_id == user:
+			return True
+
+		# Fallback to standard permission logic
+		return super().has_permission(permtype)
+		
 	def set_status_for_employee_referral(self):
 		emp_ref = frappe.get_doc("Employee Referral", self.employee_referral)
 		if self.status in ["Open", "Replied", "Hold"]:
@@ -115,4 +130,4 @@ def get_applicant_to_hire_percentage():
 	return {
 		"value": flt(total_hired) / flt(total_applicants) * 100 if total_applicants else 0,
 		"fieldtype": "Percent",
-	}
+	}
\ No newline at end of file
diff --git a/hrms/hr/doctype/job_offer/job_offer.py b/hrms/hr/doctype/job_offer/job_offer.py
index ab00804..f51dddd 100644
--- a/hrms/hr/doctype/job_offer/job_offer.py
+++ b/hrms/hr/doctype/job_offer/job_offer.py
@@ -1,7 +1,6 @@
 # Copyright (c) 2015, Frappe Technologies Pvt. Ltd. and Contributors and contributors
 # For license information, please see license.txt
 
-
 import frappe
 from frappe import _
 from frappe.model.document import Document
diff --git a/hrms/hr/doctype/job_opening/job_opening.py b/hrms/hr/doctype/job_opening/job_opening.py
index 1e28839..dc49396 100644
--- a/hrms/hr/doctype/job_opening/job_opening.py
+++ b/hrms/hr/doctype/job_opening/job_opening.py
@@ -101,6 +101,10 @@ class JobOpening(WebsiteGenerator):
 		context.no_of_applications = frappe.db.count("Job Applicant", {"job_title": self.name})
 		context.parents = [{"route": "jobs", "title": _("All Jobs")}]
 		context.posted_on = pretty_date(self.posted_on)
+		
+		# Add custom experience fields to the context
+		# context["custom_minimum_experience"] = self.custom_minimum_experience
+		# context["custom_maximum_experience"] = self.custom_maximum_experience
 
 
 def close_expired_job_openings():
diff --git a/hrms/hr/doctype/candidate_attachments/__init__.py b/hrms/hr/report/job_applicant_simple/__init__.py
similarity index 100%
rename from hrms/hr/doctype/candidate_attachments/__init__.py
rename to hrms/hr/report/job_applicant_simple/__init__.py
diff --git a/hrms/hr/report/job_applicant_simple/job_applicant_simple.json b/hrms/hr/report/job_applicant_simple/job_applicant_simple.json
new file mode 100644
index 0000000..8988356
--- /dev/null
+++ b/hrms/hr/report/job_applicant_simple/job_applicant_simple.json
@@ -0,0 +1,9 @@
+{
+ "doctype": "Report",
+ "is_standard": "Yes",
+ "module": "HR",
+ "name": "Job Applicant Simple",
+ "ref_doctype": "Job Applicant",
+ "report_name": "Job Applicant Simple",
+ "report_type": "Script Report"
+}
diff --git a/hrms/hr/report/job_applicant_simple/job_applicant_simple.py b/hrms/hr/report/job_applicant_simple/job_applicant_simple.py
new file mode 100644
index 0000000..45a0480
--- /dev/null
+++ b/hrms/hr/report/job_applicant_simple/job_applicant_simple.py
@@ -0,0 +1,19 @@
+import frappe
+
+def execute(filters=None):
+    # Define columns
+    columns = [
+        {"label": "Status", "fieldname": "status", "fieldtype": "Data", "width": 150},
+        {"label": "Number of Applicants", "fieldname": "count", "fieldtype": "Int", "width": 180},
+    ]
+
+    # Query: group Job Applicants by status
+    data = frappe.db.sql("""
+        SELECT status, COUNT(*) AS count
+        FROM `tabJob Applicant`
+        GROUP BY status
+        ORDER BY count DESC
+    """, as_dict=True)
+
+    return columns, data
+
diff --git a/hrms/hr/report/job_opening_summary/__init__.py b/hrms/hr/report/job_opening_summary/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/hrms/hr/report/job_opening_summary/job_opening_summary.json b/hrms/hr/report/job_opening_summary/job_opening_summary.json
new file mode 100644
index 0000000..ccb997e
--- /dev/null
+++ b/hrms/hr/report/job_opening_summary/job_opening_summary.json
@@ -0,0 +1,43 @@
+{
+  "doctype": "Report",
+  "name": "Job Opening Summary",
+  "module": "HR",
+  "ref_doctype": "Job Opening",
+  "report_type": "Script Report",
+  "is_standard": "Yes",
+  "roles": [
+    { "role": "HR User" },
+    { "role": "HR Manager" },
+    { "role": "System Manager" }
+  ],
+  "filters": [
+    {
+      "fieldname": "company",
+      "label": "Company",
+      "fieldtype": "Link",
+      "options": "Company",
+      "default": "Univision Technology Consulting Pvt Ltd (Demo)",
+      "reqd": 1
+    },
+    {
+      "fieldname": "status",
+      "label": "Status",
+      "fieldtype": "Select",
+      "options": "\nOpen\nClosed\nOn Hold",
+      "default": "Open",
+      "reqd": 0
+    },
+    {
+      "fieldname": "creation_from",
+      "label": "Created From",
+      "fieldtype": "Date",
+      "reqd": 0
+    },
+    {
+      "fieldname": "creation_to",
+      "label": "Created To",
+      "fieldtype": "Date",
+      "reqd": 0
+    }
+  ]
+}
diff --git a/hrms/hr/report/job_opening_summary/job_opening_summary.py b/hrms/hr/report/job_opening_summary/job_opening_summary.py
new file mode 100644
index 0000000..80fb3db
--- /dev/null
+++ b/hrms/hr/report/job_opening_summary/job_opening_summary.py
@@ -0,0 +1,87 @@
+import frappe
+from frappe import _
+
+def execute(filters=None):
+    if not filters:
+        filters = {}
+    filters = frappe._dict(filters)
+
+    columns = get_columns()
+    data = get_data(filters)
+
+    return columns, data
+
+
+def get_columns():
+    return [
+        {"label": _("Job Opening"), "fieldname": "job_opening", "fieldtype": "Link", "options": "Job Opening", "width": 150},
+        {"label": _("Job Title"), "fieldname": "job_title", "fieldtype": "Data", "width": 200},
+        {"label": _("Status"), "fieldname": "status", "fieldtype": "Data", "width": 100},
+        {"label": _("Positions"), "fieldname": "number_of_positions", "fieldtype": "Int", "width": 100},
+        {"label": _("Total Applicants"), "fieldname": "total_applicants", "fieldtype": "Int", "width": 120},
+        {"label": _("Created On"), "fieldname": "creation", "fieldtype": "Datetime", "width": 150},
+        {"label": _("Last Modified"), "fieldname": "modified", "fieldtype": "Datetime", "width": 150}
+    ]
+
+
+def get_data(filters):
+    data = []
+    job_openings = get_job_openings(filters)
+
+    # Get applicants mapped by job_title (linking to Job Opening)
+    jo_ja_map = get_applicants([jo.name for jo in job_openings])
+
+    for jo in job_openings:
+        row = {
+            "job_opening": jo.name,
+            "job_title": jo.job_title,
+            "status": jo.status,
+            "number_of_positions": jo.number_of_positions or 0,
+            "total_applicants": len(jo_ja_map.get(jo.name, [])),
+            "creation": jo.creation,
+            "modified": jo.modified
+        }
+        data.append(row)
+
+    return data
+
+
+def get_job_openings(filters):
+    conditions = [["company", "=", filters.company]]
+
+    if filters.get("status"):
+        conditions.append(["status", "=", filters.status])
+    if filters.get("creation_from"):
+        conditions.append(["creation", ">=", filters.creation_from])
+    if filters.get("creation_to"):
+        conditions.append(["creation", "<=", filters.creation_to])
+    if filters.get("job_title"):
+        conditions.append(["job_title", "like", f"%{filters.job_title}%"])
+
+    job_openings = frappe.get_all(
+        "Job Opening",
+        filters=conditions,
+        fields=["name", "job_title", "status", "planned_vacancies as number_of_positions", "creation", "modified"],
+        order_by="creation desc",
+        limit=100
+    )
+
+    return job_openings
+
+
+def get_applicants(jo_list):
+    jo_ja_map = {}
+    if not jo_list:
+        return jo_ja_map
+
+    # Map applicants using job_title (link to Job Opening)
+    applicants = frappe.get_all(
+        "Job Applicant",
+        filters=[["job_title", "in", jo_list]],
+        fields=["name", "job_title", "applicant_name", "status"]
+    )
+
+    for ja in applicants:
+        jo_ja_map.setdefault(ja.job_title, []).append(ja)
+
+    return jo_ja_map
diff --git a/hrms/hr/report/rec_sum/__init__.py b/hrms/hr/report/rec_sum/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/hrms/hr/report/rec_sum/rec_sum.json b/hrms/hr/report/rec_sum/rec_sum.json
new file mode 100644
index 0000000..90546de
--- /dev/null
+++ b/hrms/hr/report/rec_sum/rec_sum.json
@@ -0,0 +1,67 @@
+{
+ "add_total_row": 0,
+ "add_translate_data": 0,
+ "columns": [],
+ "creation": "2025-11-11 15:06:51.835980",
+ "disabled": 0,
+ "docstatus": 0,
+ "doctype": "Report",
+ "filters": [
+  {
+   "default": "Univision Technology Consulting Pvt Ltd (Demo)",
+   "fieldname": "company",
+   "fieldtype": "Link",
+   "label": "Company",
+   "mandatory": 0,
+   "options": "Company",
+   "wildcard_filter": 0
+  },
+  {
+   "default": "Open",
+   "fieldname": "status",
+   "fieldtype": "Select",
+   "label": "Status",
+   "mandatory": 0,
+   "options": "\nOpen\nClosed\nOn Hold",
+   "wildcard_filter": 0
+  },
+  {
+   "fieldname": "creation_from",
+   "fieldtype": "Date",
+   "label": "Created From",
+   "mandatory": 0,
+   "wildcard_filter": 0
+  },
+  {
+   "fieldname": "creation_to",
+   "fieldtype": "Date",
+   "label": "Created To",
+   "mandatory": 0,
+   "wildcard_filter": 0
+  }
+ ],
+ "idx": 0,
+ "is_standard": "Yes",
+ "letterhead": null,
+ "modified": "2025-11-11 15:07:19.773475",
+ "modified_by": "Administrator",
+ "module": "HR",
+ "name": "rec_sum",
+ "owner": "Administrator",
+ "prepared_report": 0,
+ "ref_doctype": "Job Opening",
+ "report_name": "rec_sum",
+ "report_type": "Script Report",
+ "roles": [
+  {
+   "role": "HR User"
+  },
+  {
+   "role": "HR Manager"
+  },
+  {
+   "role": "System Manager"
+  }
+ ],
+ "timeout": 0
+}
\ No newline at end of file
diff --git a/hrms/hr/report/recruitment_analytics/recruitment_analytics.json b/hrms/hr/report/recruitment_analytics/recruitment_analytics.json
index 30a8e17..478920f 100644
--- a/hrms/hr/report/recruitment_analytics/recruitment_analytics.json
+++ b/hrms/hr/report/recruitment_analytics/recruitment_analytics.json
@@ -1,13 +1,16 @@
 {
  "add_total_row": 0,
+ "add_translate_data": 0,
+ "columns": [],
  "creation": "2020-05-14 16:28:45.743869",
- "disable_prepared_report": 0,
  "disabled": 0,
  "docstatus": 0,
  "doctype": "Report",
+ "filters": [],
  "idx": 0,
  "is_standard": "Yes",
- "modified": "2020-05-14 16:28:45.743869",
+ "letterhead": null,
+ "modified": "2025-11-11 12:14:40.851016",
  "modified_by": "Administrator",
  "module": "HR",
  "name": "Recruitment Analytics",
@@ -23,5 +26,6 @@
   {
    "role": "HR User"
   }
- ]
+ ],
+ "timeout": 0
 }
\ No newline at end of file
diff --git a/hrms/hr/report/summary___job_applicants/__init__.py b/hrms/hr/report/summary___job_applicants/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/hrms/hr/report/summary___job_applicants/summary___job_applicants.json b/hrms/hr/report/summary___job_applicants/summary___job_applicants.json
new file mode 100644
index 0000000..3ad503e
--- /dev/null
+++ b/hrms/hr/report/summary___job_applicants/summary___job_applicants.json
@@ -0,0 +1,38 @@
+{
+ "add_total_row": 0,
+ "add_translate_data": 0,
+ "columns": [],
+ "creation": "2025-11-06 18:32:04.189957",
+ "disabled": 0,
+ "docstatus": 0,
+ "doctype": "Report",
+ "filters": [],
+ "idx": 0,
+ "is_standard": "Yes",
+ "letterhead": null,
+ "modified": "2025-11-11 00:49:55.555734",
+ "modified_by": "Administrator",
+ "module": "HR",
+ "name": "Summary - Job Applicants",
+ "owner": "Administrator",
+ "prepared_report": 0,
+ "ref_doctype": "Job Applicant",
+ "report_name": "Summary - Job Applicants",
+ "report_script": "def execute(filters=None):\r\n    columns = [\r\n        {\"label\": \"Applicant Name\", \"fieldname\": \"applicant_name\", \"fieldtype\": \"Data\", \"width\": 200},\r\n    ]\r\n\r\n    data = [\r\n        {\"applicant_name\": \"Test Applicant\"}\r\n    ]\r\n\r\n    return columns, data\r\n",
+ "report_type": "Script Report",
+ "roles": [
+  {
+   "role": "HR User"
+  },
+  {
+   "role": "Employee Self Service"
+  },
+  {
+   "role": "Website Manager"
+  },
+  {
+   "role": "Administrator"
+  }
+ ],
+ "timeout": 0
+}
\ No newline at end of file
diff --git a/hrms/hr/report/summary___job_applicants/summary___job_applicants.py b/hrms/hr/report/summary___job_applicants/summary___job_applicants.py
new file mode 100644
index 0000000..f3361ad
--- /dev/null
+++ b/hrms/hr/report/summary___job_applicants/summary___job_applicants.py
@@ -0,0 +1,17 @@
+import frappe
+
+def execute(filters=None):
+    columns = [
+        {"label": "Applicant ID", "fieldname": "name", "fieldtype": "Link", "options": "Job Applicant", "width": 150},
+        {"label": "Applicant Name", "fieldname": "applicant_name", "fieldtype": "Data", "width": 200},
+        {"label": "Status", "fieldname": "status", "fieldtype": "Data", "width": 120},
+        {"label": "Creation", "fieldname": "creation", "fieldtype": "Datetime", "width": 180},
+    ]
+
+    # Fetch Job Applicant data
+    data = frappe.get_all(
+        "Job Applicant",
+        fields=["name", "applicant_name", "status", "creation"]
+    )
+
+    return columns, data
diff --git a/hrms/hr/web_form/job_application/job_application.json b/hrms/hr/web_form/job_application/job_application.json
index 512ba5c..e19bef1 100644
--- a/hrms/hr/web_form/job_application/job_application.json
+++ b/hrms/hr/web_form/job_application/job_application.json
@@ -1,15 +1,14 @@
 {
- "accept_payment": 0,
  "allow_comments": 1,
  "allow_delete": 0,
  "allow_edit": 1,
  "allow_incomplete": 0,
  "allow_multiple": 1,
  "allow_print": 0,
- "amount": 0.0,
- "amount_based_on_field": 0,
+ "anonymous": 0,
  "apply_document_permissions": 0,
  "client_script": "frappe.web_form.on('resume_link', (field, value) => {\n    if (!frappe.utils.is_url(value)) {\n        frappe.msgprint(__('Resume link not valid'));\n    }\n});\n",
+ "condition_json": "[]",
  "creation": "2016-09-10 02:53:16.598314",
  "doc_type": "Job Applicant",
  "docstatus": 0,
@@ -17,20 +16,19 @@
  "idx": 0,
  "introduction_text": "",
  "is_standard": 1,
- "login_required": 0,
+ "list_columns": [],
+ "login_required": 1,
  "max_attachment_size": 0,
- "modified": "2020-10-07 19:27:17.143355",
+ "modified": "2025-11-25 22:26:18.882557",
  "modified_by": "Administrator",
  "module": "HR",
  "name": "job-application",
  "owner": "Administrator",
- "published": 1,
+ "published": 0,
  "route": "job_application",
- "route_to_success_link": 0,
  "show_attachments": 0,
- "show_in_grid": 0,
+ "show_list": 1,
  "show_sidebar": 1,
- "sidebar_items": [],
  "success_message": "Thank you for applying.",
  "success_url": "/jobs",
  "title": "Job Application",
diff --git a/hrms/templates/generators/job_opening.html b/hrms/templates/generators/job_opening.html
index eacfdb6..4bff8f1 100644
--- a/hrms/templates/generators/job_opening.html
+++ b/hrms/templates/generators/job_opening.html
@@ -1,4 +1,14 @@
 {% extends "templates/web.html" %}
+{% block header %}
+<h3 class="mt-0 mb-10 jobs-page">{{ _("Job Openings") }}</h3>
+<!-- Custom Icons -->
+<svg style="display:none;">
+	<symbol id="icon-experience" viewBox="0 0 24 24">
+		<path d="M4 7h16v2H4zM4 11h16v2H4zM4 15h16v2H4z" stroke="currentColor" stroke-width="1.5" fill="none" />
+		<path d="M8 3h8v4H8z" stroke="currentColor" stroke-width="1.5" fill="none"/>
+	</symbol>
+</svg>
+{% endblock header %}
 {% block page_content %}
 
 <div class="py-12">
@@ -21,23 +31,18 @@
 		</div>
 		<div class="col-md-3 flex">
 			<div class="ml-auto d-none d-md-block">
-				{%- if status == "Open" -%}
-					<a
-						class="btn btn-primary btn-lg"
-						href="/{{ job_application_route if job_application_route else 'job_application' }}/new?job_title={{ name }}"
-					>
-						{{ _("Apply Now") }}
-					</a>
-				{%- else -%}
-					<div
-						class="py-4 px-10 font-weight-bold text-nowrap"
-						style="background: var(--bg-gray);
-							font-size: 1.1rem;
-							border-radius: var(--border-radius)"
-					>
-						{{ _("Opening closed.") }}
-					</div>
-				{% endif %}
+			{% if status == "Open" %}
+				<a class="btn btn-primary btn-lg"
+				href="/{{ job_application_route or 'job-application-univision' }}/new?job_title={{ name }}&source=Website%20Listing">
+				{{ _("Apply Now") }}
+				</a>
+			{% else %}
+				<div class="py-4 px-10 font-weight-bold text-nowrap"
+					style="background: var(--bg-gray); font-size: 1.1rem; border-radius: var(--border-radius)">
+					{{ _("Opening closed.") }}
+				</div>
+			{% endif %}
+
 			</div>
 		</div>
 	</div>
@@ -90,7 +95,35 @@
 					</div>
 				</div>
 			{% endif %}
+{%- if custom_minimum_experience or custom_maximum_experience -%}
+<div class="col-12 col-md-4 mb-6 mb-md-8">
+	<div class="flex flex-row align-items-center">
+		<div class="rounded-circle p-4" style="background: var(--gray-50)">
+			<svg
+				class="icon"
+				style="height: 28px; width: 28px; --icon-stroke: var(--gray-700)"
+			>
+				<use href="#icon-experience"></use>
+			</svg>
+		</div>
+		<div class="ml-5">
+			<div class="text-secondary text-uppercase" style="font-size: 11px">
+				Experience Required
+			</div>
 
+			<div class="font-weight-bold">
+				{%- if custom_minimum_experience and custom_maximum_experience -%}
+					{{ custom_minimum_experience }} – {{ custom_maximum_experience }} years
+				{%- elif custom_minimum_experience -%}
+					Minimum {{ custom_minimum_experience }} years
+				{%- elif custom_maximum_experience -%}
+					Up to {{ custom_maximum_experience }} years
+				{%- endif -%}
+			</div>
+		</div>
+	</div>
+</div>
+{%- endif -%}
 			{%- if publish_salary_range -%}
 				<div class="col-12 col-md-4 mb-6 mb-md-8">
 					<div class="flex flex-row align-items-center">
diff --git a/hrms/www/candidate_upload/candidate_upload.html b/hrms/www/candidate_upload/candidate_upload.html
index 0493bee..2a68521 100644
--- a/hrms/www/candidate_upload/candidate_upload.html
+++ b/hrms/www/candidate_upload/candidate_upload.html
@@ -21,7 +21,7 @@ body {
 }
 
 /* Header */
-h2 {
+.candidate-heading {
   display: flex;
   align-items: center;
   gap: 10px;
@@ -93,11 +93,47 @@ h2 {
   transform: translateY(-1px);
   box-shadow: 0 4px 10px rgba(13,110,253,0.4);
 }
+
+/* Mobile responsiveness */
+@media (max-width: 768px) {
+  .container {
+    padding: 1rem !important;
+  }
+
+  .candidate-heading {
+    font-size: 1.25rem;
+    gap: 6px;
+  }
+
+  .info-box {
+    padding: 0.75rem 1rem;
+    font-size: 0.9rem;
+  }
+
+  .table th, .table td {
+    font-size: 0.8rem;
+    padding: 0.5rem 0.25rem;
+  }
+
+  #save-all {
+    width: 100%;
+    padding: 0.5rem 0;
+  }
+
+  .uploaded-files a {
+    font-size: 0.85rem;
+    padding: 2px 6px;
+  }
+  
+  .filepond--root {
+    font-size: 0.85rem;
+  }
+}
 </style>
 
 <div class="container py-5" style="max-width: 950px;">
   <div class="card border-0 p-4 p-md-5">
-    <h2 class="fw-bold mb-4">📄 Candidate Document Upload</h2>
+    <h2 class="candidate-heading fw-bold mb-4">📄 Candidate Document Upload</h2>
 
     <div id="loading" class="text-muted text-center py-5 fs-5">
       ⏳ Loading your records...
@@ -168,14 +204,17 @@ frappe.ready(() => {
       tbody.innerHTML = "";
 
       documents.forEach(doc => {
-        const fileList = [...new Set(doc.file_list || [])]; // remove duplicates
+        const fileList = [...new Set(doc.file_list || [])];
         const existingFilesHtml = fileList.length
           ? fileList.map(f => `<a href="/files/${f.split("/").pop()}" target="_blank">📎 ${f.split("/").pop()}</a>`).join("")
           : `<span class="text-muted">No files uploaded yet</span>`;
 
         tbody.innerHTML += `
           <tr data-name="${doc.name}" data-file="${fileList.join(';')}">
-            <td><strong>${doc.document_type_name || doc.document_type}</strong></td>
+            <td>
+              <strong>${doc.document_name || doc.document_type}</strong>
+              ${doc.description ? `<br><small class="text-muted">(${doc.description})</small>` : ''}
+            </td>
             <td>
               <div class="uploaded-files mb-2">${existingFilesHtml}</div>
               <input type="file" class="filepond file-input" multiple accept="application/pdf,image/*">
@@ -190,11 +229,9 @@ frappe.ready(() => {
 
       document.querySelectorAll('.filepond').forEach(input => {
         const row = input.closest("tr");
-        const pond = FilePond.create(input, {
-          allowMultiple: true,
-          maxFiles: 10
-        });
+        const pond = FilePond.create(input, { allowMultiple: true, maxFiles: 10 });
 
+        // --- When a file is added ---
         pond.on('addfile', (error, file) => {
           if (error) return;
           const child_name = row.dataset.name;
@@ -213,53 +250,92 @@ frappe.ready(() => {
           .then(res => res.json())
           .then(json => {
             const fileUrl = json.message.file_url;
-            let existingFiles = row.dataset.file ? row.dataset.file.split(";") : [];
-
-            // Check if file already exists; replace if needed
-            if (!existingFiles.includes(fileUrl)) {
-              existingFiles.push(fileUrl);
-            }
+            const filename = fileUrl.split("/").pop();
 
+            // --- Update dataset ---
+            let existingFiles = row.dataset.file ? row.dataset.file.split(";") : [];
+            if (!existingFiles.includes(fileUrl)) existingFiles.push(fileUrl);
             row.dataset.file = existingFiles.join(";");
 
-            row.querySelector(".uploaded-files").innerHTML = existingFiles.map(f =>
-              `<a href="/files/${f.split("/").pop()}" target="_blank">📎 ${f.split("/").pop()}</a>`
-            ).join("");
-
-            frappe.show_alert({ message: `Uploaded successfully`, indicator: "green" });
+            // --- Add to custom uploaded file list ---
+            const uploadedFilesDiv = row.querySelector(".uploaded-files");
+            const fileItemHTML = `
+              <span class="uploaded-file-item" data-file="${fileUrl}">
+                <a href="/files/${filename}" target="_blank">📎 ${filename}</a>
+                <span class="remove-file text-danger" style="cursor:pointer; margin-left:6px;">❌</span>
+              </span>
+            `;
+            uploadedFilesDiv.insertAdjacentHTML("beforeend", fileItemHTML);
+
+            // --- Add remove-file listener ---
+            uploadedFilesDiv.querySelectorAll('.remove-file').forEach(btn => {
+              btn.onclick = function() {
+                const fileSpan = this.closest('.uploaded-file-item');
+                const fileUrl = fileSpan.dataset.file;
+                const filename = fileUrl.split("/").pop();
+
+                // Remove from dataset
+                let existing = row.dataset.file ? row.dataset.file.split(";") : [];
+                existing = existing.filter(f => f !== fileUrl);
+                row.dataset.file = existing.join(";");
+
+                // Remove FilePond preview (sync)
+                const pondItem = Array.from(pond.getFiles()).find(f => f.filename === filename);
+                if (pondItem) pond.removeFile(pondItem.id);
+
+                // Remove UI item
+                fileSpan.remove();
+
+                // Delete from backend
+                frappe.call({
+                  method: "frappe.core.doctype.file.file.delete_file",
+                  args: { fid: `/files/${filename}` },
+                  callback: () => frappe.show_alert({ message: `${filename} deleted`, indicator: "red" })
+                });
+              };
+            });
+
+            frappe.show_alert({ message: `${filename} uploaded successfully`, indicator: "green" });
           })
-          .catch(err => {
-            console.error("Upload error:", err);
-            frappe.msgprint(`File upload failed: ${file.filename}`);
+          .catch(() => frappe.msgprint(`File upload failed: ${file.filename}`));
+        });
+
+        // --- When FilePond removes a file (sync with custom list) ---
+        pond.on('removefile', (error, file) => {
+          if (error) return;
+          const filename = file.filename;
+          const fileUrl = `/files/${filename}`;
+
+          // Remove from dataset
+          let existingFiles = row.dataset.file ? row.dataset.file.split(";") : [];
+          existingFiles = existingFiles.filter(f => !f.endsWith(filename));
+          row.dataset.file = existingFiles.join(";");
+
+          // Remove from uploaded-files list
+          const fileSpan = row.querySelector(`.uploaded-file-item[data-file="${fileUrl}"]`);
+          if (fileSpan) fileSpan.remove();
+
+          // Delete from backend
+          frappe.call({
+            method: "frappe.core.doctype.file.file.delete_file",
+            args: { fid: fileUrl },
+            callback: () => frappe.show_alert({ message: `${filename} deleted`, indicator: "red" })
           });
         });
       });
 
       document.getElementById("save-all").addEventListener("click", () => {
-        const rows = document.querySelectorAll("#documents-table tr");
-        const updates = [];
-
-        rows.forEach(row => {
-          updates.push({
-            name: row.dataset.name,
-            file: row.dataset.file || null,
-            comments: row.querySelector(".comment").value
-          });
-        });
+        const updates = Array.from(document.querySelectorAll("#documents-table tr")).map(row => ({
+          name: row.dataset.name,
+          file: row.dataset.file || null,
+          comments: row.querySelector(".comment").value
+        }));
 
         frappe.call({
           method: "hrms.www.candidate_upload.candidate_upload.update_candidate_documents",
-          args: {
-            parent_name: candidate.name,
-            documents: JSON.stringify(updates)
-          },
-          callback: () => {
-            frappe.show_alert({ message: "All changes saved successfully!", indicator: "green" });
-          },
-          error: (err) => {
-            console.error("Failed to save", err);
-            frappe.msgprint("Failed to update documents.");
-          }
+          args: { parent_name: candidate.name, documents: JSON.stringify(updates) },
+          callback: () => frappe.show_alert({ message: "All changes saved successfully!", indicator: "green" }),
+          error: () => frappe.msgprint("Failed to update documents.")
         });
       });
     }
diff --git a/hrms/www/candidate_upload/candidate_upload.py b/hrms/www/candidate_upload/candidate_upload.py
index a82ee8c..b6d01f4 100755
--- a/hrms/www/candidate_upload/candidate_upload.py
+++ b/hrms/www/candidate_upload/candidate_upload.py
@@ -1,7 +1,6 @@
 import frappe
 from frappe import _
 import json
-import traceback
 import os
 
 @frappe.whitelist(allow_guest=False)
@@ -9,10 +8,7 @@ def get_candidate_upload():
     """Fetch Candidate Document Upload record for logged-in user"""
     try:
         user = frappe.session.user
-        frappe.logger().info(f"🟩 [get_candidate_upload] Current session user: {user}")
-
         email = frappe.db.get_value("User", user, "email")
-        frappe.logger().info(f"📧 Email for user {user}: {email}")
 
         if not email:
             frappe.throw(_("No email found for logged-in user."))
@@ -32,11 +28,11 @@ def get_candidate_upload():
         candidate_documents = frappe.get_all(
             "Candidate Document",
             filters={"parent": candidate_doc.name},
-            fields=["name", "document_type", "document_type_name", "file", "comments"],
+            fields=["name", "document_type", "document_name", "description", "file", "comments"],
             order_by="idx asc"
         )
 
-        # Split multi-files for frontend; store only file names in `file` but full path in file_list
+        # Prepare file lists for frontend
         for doc in candidate_documents:
             if doc.get("file") and ";" in doc["file"]:
                 full_files = doc["file"].split(";")
@@ -55,47 +51,32 @@ def get_candidate_upload():
         }
 
     except Exception as e:
-        frappe.logger().error("❌ Error in get_candidate_upload:")
-        frappe.logger().error(traceback.format_exc())
+        frappe.logger().error(f"Error in get_candidate_upload: {str(e)}")
         frappe.throw(f"Error while fetching candidate upload: {str(e)}")
 
 
 @frappe.whitelist(allow_guest=False)
 def update_candidate_documents(parent_name, documents):
-    """Update candidate uploaded files and attach them to parent document (multiple files, file names only in 'file')"""
+    """Update candidate uploaded files and attach them to parent document"""
     try:
-        frappe.logger().info(f"🟦 [update_candidate_documents] Called for parent: {parent_name}")
-        frappe.logger().info(f"📦 Raw input documents: {documents}")
-
         documents = json.loads(documents)
-        frappe.logger().info(f"📋 Parsed documents JSON: {documents}")
-
         candidate = frappe.get_doc("Candidate Document Upload", parent_name)
 
-        updated_count = 0
         for doc in documents:
-            frappe.logger().info(f"🔍 Processing child document: {doc.get('name')}")
-            found = False
-
             for child in candidate.candidate_documents:
                 if child.name == doc.get("name"):
-                    found = True
-                    frappe.logger().info(f"🧩 Found matching child row: {child.name}")
 
                     # Handle multiple files
                     if doc.get("file"):
                         file_value = doc.get("file")
-
-                        # Convert lists to semicolon-separated string
                         if isinstance(file_value, list):
                             file_value = ";".join(file_value)
 
-                        # Store **only file names** in child table
+                        # Store only file names in child table
                         file_names_only = ";".join([os.path.basename(f) for f in file_value.split(";")])
-                        frappe.logger().info(f"📁 Updating file names for {child.name} → {file_names_only}")
                         child.db_set("file", file_names_only)
 
-                        # Attach all files in File doctype (full path remains)
+                        # Attach files in File doctype
                         for file_path in file_value.split(";"):
                             file_path = file_path.strip()
                             if not file_path:
@@ -114,28 +95,17 @@ def update_candidate_documents(parent_name, documents):
                                         or file_doc.attached_to_name != parent_name):
                                     file_doc.db_set("attached_to_doctype", "Candidate Document Upload")
                                     file_doc.db_set("attached_to_name", parent_name)
-                                    frappe.logger().info(f"📎 Attached file {file_doc.name} → parent {parent_name}")
-                            else:
-                                frappe.logger().warning(f"⚠️ File URL {file_path} not found in File doctype")
 
                     # Update comments
                     if doc.get("comments"):
-                        frappe.logger().info(f"💬 Updating comments for {child.name} → {doc.get('comments')}")
                         child.db_set("comments", doc.get("comments"))
-
-                    updated_count += 1
                     break
 
-            if not found:
-                frappe.logger().warning(f"⚠️ No matching child found for name: {doc.get('name')}")
-
         candidate.save(ignore_permissions=True)
         frappe.db.commit()
-        frappe.logger().info("✅ Documents updated successfully and committed to DB")
 
-        return {"success": True, "message": f"Documents updated successfully. Total updated: {updated_count}"}
+        return {"success": True, "message": "Documents updated successfully."}
 
     except Exception as e:
-        frappe.logger().error("❌ Error in update_candidate_documents:")
-        frappe.logger().error(traceback.format_exc())
+        frappe.logger().error(f"Error in update_candidate_documents: {str(e)}")
         frappe.throw(f"Update failed: {str(e)}")
diff --git a/hrms/www/jobs/index.html b/hrms/www/jobs/index.html
index 5e85dfb..ffa5f6b 100644
--- a/hrms/www/jobs/index.html
+++ b/hrms/www/jobs/index.html
@@ -356,4 +356,4 @@
 	<div  id="overlay" name="close-filters-drawer" class="overlay d-lg-none"></div>
 
 </div>
-{% endblock page_content %}
+{% endblock page_content %}
\ No newline at end of file
diff --git a/hrms/www/jobs/index.js b/hrms/www/jobs/index.js
index 01a56aa..2a9bbb9 100644
--- a/hrms/www/jobs/index.js
+++ b/hrms/www/jobs/index.js
@@ -135,4 +135,4 @@ function scroll_up_and_execute(callback) {
 		behavior: "smooth",
 	});
 	window.addEventListener("scroll", execute_after_scrolling_up);
-}
+}
\ No newline at end of file
diff --git a/hrms/www/jobs/index.py b/hrms/www/jobs/index.py
index f8f100f..0cc1e0b 100644
--- a/hrms/www/jobs/index.py
+++ b/hrms/www/jobs/index.py
@@ -128,4 +128,4 @@ def get_filters_txt_sort_offset(page_len=20):
 		elif d == "page":
 			offset = (int(args["page"][0]) - 1) * page_len
 
-	return filters, txt, sort, offset
+	return filters, txt, sort, offset
\ No newline at end of file
